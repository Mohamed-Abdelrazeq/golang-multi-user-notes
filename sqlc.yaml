version: "2"
sql:
  - engine: "postgresql"
    queries: "./queries"
    schema: "./schema"
    gen:
      go:
        out: "./db"
        json_tags_case_style: snake
        emit_json_tags: true

        