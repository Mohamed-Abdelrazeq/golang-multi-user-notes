version: "2"
sql:
  - engine: "postgresql"
    queries: "./db/queries"
    schema: "./db/schema"
    gen:
      go:
        out: "./db/internals"
        json_tags_case_style: snake
        emit_json_tags: true

        